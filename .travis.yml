language: go

go:
  - 1.7
  - 1.8
  - master

before_install:
  - go get -u github.com/golang/lint/golint
  - go get -u github.com/mvdan/interfacer/cmd/interfacer

script:
 - make lint
 - make test
 - make build

matrix:
  allow_failures:
    - go: master
